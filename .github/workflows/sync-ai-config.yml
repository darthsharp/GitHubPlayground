name: Sync AI Configuration

on:
  workflow_dispatch:
    inputs:
      create-pull-request:
        description: 'Create a pull request instead of committing directly to the branch'
        required: false
        type: boolean
        default: true
      ai-base-version:
        description: 'Version/branch of ai-base to use'
        required: false
        type: string
        default: 'feature/jstooltodotnet'
#  schedule:
#    - cron: '0 0 * * 0'  # Weekly on Sundays

jobs:
  sync:
    uses: CreativeCodersTeam/ai-base/.github/workflows/sync-ai-config.yml@feature/jstooltodotnet
    with:
      languages: 'csharp,typescript'
      ai-systems: 'copilot,claude,junie'
      ai-base-version: ${{ github.event.inputs.ai-base-version }}
      create-pull-request: ${{ github.event.inputs.create-pull-request }}